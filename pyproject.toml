[project]
name = "stan"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "bridgestan>=2.9.0",
    "cmdstanpy>=1.3.0",
    "nutpie>=0.16.11",
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
]

[dependency-groups]
dev = [
    "sympy>=1.14.0",
]
